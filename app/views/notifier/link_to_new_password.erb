Dear <%= @user.full_name.present? ? @user.full_name : @user.login %>!

Please click at the following link if you do want to reset your password:

<%= url_for :controller => 'users', :action => 'resetpassword', :id => @user.password_reset_code, :only_path => false %>
